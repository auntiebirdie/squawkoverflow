name: SQUAWKcicd

on:
  push

jobs:
  notify-begin:
    runs-on: ubuntu-latest
    steps:
    - name: Post notification
      run: curl -X POST -D '{"content": "Deployment started"}' -H "Content-Type:application/json" "https://discord.com/api/webhooks/${{ secrets.WEBHOOK_ID }}/${{ secrets.WEBHOOK_TOKEN }}"
