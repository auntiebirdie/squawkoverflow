<%- include('../header.ejs') %>
  
  <div id="errorMessage" class="bg-warning text-dark mt-5 p-3 w-75 rounded <%= locals.displayError ? '' : 'd-none' %>"><%= locals.displayError %></div>

  <div class="text-center pb-5 mx-auto w-75">
    <h1 class="bg-light mt-5 mb-0 p-4 border-bottom border-primary border-5">
      <strong>Report a Bug</strong>
    </h1>

    <div class="bg-light p-4">
      <p>Report the bug you found using this form to receive a bug on SQUAWKoverflow that you can feed to your birds or use as bait to attract wishlisted birds.</p>

      <form method="POST">
        <p>The bug will be awarded to:

         <% if (locals.loggedInUser) { %>
            <strong><%= loggedInUser.username %> <small>(<%= loggedInUser.id %>)</small></strong>
            <input type="hidden" value="<%= loggedInUser.username %>" name="memberName">
            <input type="hidden" value="<%= loggedInUser.id %>" name="memberId">
         <% } else { %>
           <em>No member detected.</em>
         <% } %>
        </p> 

        <p>Please try to describe what happened or share any other information about the bug you found. <em>(at least 25 characters)</em></p>

        <p><textarea name="description" minlength="25" class="form-control"></textarea></p>

        <p><button class="btn btn-dark">🐛 Submit Bug Report</button></p>
      </form>
    </div>
  </div>

<%- include('../footer.ejs') %>
