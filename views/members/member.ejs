<%- include('../header.ejs') %>

  <%- include ('../partials/page-title.ejs', { title : member.username }) %>

  <div class="row">
    <div class="col-sm-6 col-md-2 text-center mb-3">
      <img class="member-avatar" src="<%= member.avatar %>" data-id="<%= member.id %>" onerror="missingMemberAvatar(this)">
    </div>
    <div class="col-sm-6 col-md-4 text-center text-md-start mb-3">
      <h4>
        <% if (member.aviary._total <= 0) { %>
          Aspiring
        <% } else if (member.aviary._total < 10) { %>
          Beginner
        <% } else if (member.aviary._total < 100) { %>
          Novice
        <% } else if (member.aviary._total < 500) { %>
          Dedicated
        <% } else if (member.aviary._total < 1000) { %>
          Greater
        <% } else if (member.aviary._total < 5000) { %>
          Superb
        <% } else if (member.aviary._total < 10000) { %>
          Penultimate
        <% } else if (member.aviary._total > 10000) { %>
          Ultimate
        <% } %>

        <%= member.tier.name %>
      </h4>
      <p><%= (member.aviary._total * 1).toLocaleString("en-US") %> birds collected</p>

      <h5>Joined</h5>
      <p><%= new Date(member.joinedAt * 1).toLocaleString('en-US', { dateStyle: "long" }) %></p>
    </div>
    <div class="col-md-6 text-center text-md-end">
        <p><a href="/aviary/<%= member.id %>" class="btn btn-primary">🐦 View Aviary</a></p>
      <% if (member.wishlist && !member.settings.privacy?.includes('wishlist')) { %>
        <p><a href="/wishlist/<%= member.id %>" class="btn btn-primary">❤️ View Wishlist</a></p>
      <% } %>
      <% if (locals.loggedInUser && loggedInUser.id != member.id && !member.settings.privacy?.includes('gifts')) { %>
        <p><a href="/members/<%= member.id %>/gift" class="btn btn-primary">🎁 Send Gift</a></p>
      <% } %>
    </div>
  </div>

  <ul class="nav nav-tabs" id="profileTabs" role="tablist">
    <li class="nav-item" role="presentation">
      <button class="nav-link active" id="birdy-buddy-tab" data-bs-toggle="tab" data-bs-target="#birdy-buddy" type="button" role="tab" aria-controls="home" aria-selected="true">Birdy Buddy</button>
    </li>
    <li class="nav-item" role="presentation">
      <button class="nav-link" id="featured-flock-tab" data-bs-toggle="tab" data-bs-target="#featured-flock" type="button" role="tab" aria-controls="profile" aria-selected="false">Featured Flock</button>
    </li>
    <li class="nav-item" role="presentation">
      <button class="nav-link" id="collection-progress-tab" data-bs-toggle="tab" data-bs-target="#collection-progress" type="button" role="tab" aria-controls="contact" aria-selected="false">Collection Progress</button>
    </li>
  </ul>
  <div class="tab-content py-4 px-3" id="profileTabsContent">
    <div class="tab-pane fade show active" id="birdy-buddy" role="tabpanel" aria-labelledby="birdy-buddy-tab">
     <% if (member.birdyBuddy?.illustration) { %>

      <div class="row">
        <div class="col-md-6 text-center">
          <div class="birdybuddy mb-3">
            <img src="<%= member.birdyBuddy.illustration.image %>">
            <span class="message border border-1 border-secondary" style="display: none;"></span>
          </div>
        </div>
        <div class="col-md-6">
          <h4><a class="text-decoration-none" href="/birdypet/<%= member.birdyBuddy.id %>"><%= member.birdyBuddy.nickname ? member.birdyBuddy.nickname : member.birdyBuddy.bird.name %></a></h4>

          <%- include('../partials/friendshipMeter.ejs', { friendship : member.birdyBuddy.friendship }) %>

          <div>
            <% if (member.birdyBuddy.description) { %>
            <div class="py-4 ps-4">
              <% for (var para of member.birdyBuddy.description.split("\r\n")) { %>
                <p><%= para %></p>
              <% } %>
            </div>
            <% } %>

            <% if (locals.loggedInUser && loggedInUser.bugs > 0) { %>
               <% if (member.birdyBuddy.friendship >= 100) { %>
                 <p><em>You have <%= loggedInUser.bugs %> bug<%= loggedInUser.bugs != 1 ? "s" : "" %>, but <%= member.birdyBuddy.nickname ? member.birdyBuddy.nickname : member.birdyBuddy.species %> is full!</em></p>
               <% } else { %>
                 <p><button id="feedBug" class="btn btn-primary" role="button" data-birdypet="<%= member.birdyBuddy.id %>">🐛 Feed a Bug (you have <span><%= loggedInUser.bugs %></span>)</button></p>
               <% } %>
            <% } %>
          </div>
        </div>
      </div>
     <% } else { %>
      <em>This member hasn't selected a Birdy Buddy.</em>
     <% } %>
    </div>
    <div class="tab-pane fade" id="featured-flock" role="tabpanel" aria-labelledby="featured-flock-tab">
      <% if (member.featuredFlock) { %>
        <div id="lazyLoader" class="row" data-endpoint="/api/aviary" data-params='{"member":"<%= member.id %>","flock":"<%= member.featuredFlock.id %>"}'></div>

        <%- include('../scripts/lazyloader.ejs') %>
     <% } else { %>
        <em>This member hasn't selected a flock to feature.</em>
      <% } %>
    </div>
    <div class="tab-pane fade" id="collection-progress" role="tabpanel" aria-labelledby="collection-progress-tab">
     <% if (member.aviary._total > 0) { %>
      <div class="row">
        <% for (var family of member.families) { %>
          <% if (family.owned > 0) { %>
            <div class="col-6 col-md-4 col-lg-3 mb-4">
              <h5><a href="/birdypedia?family=<%= family.value %>"><%= family.value %></a></h5>
              <h6 class="small"><small>(<%= family.label.split(' (').pop() %></small></h6>
              <% var prct = family.owned / family.total * 100; %>
              <div class="progress mx-2">
                <div class="progress-bar<%= prct == 100 ? " bg-success" : "" %>" style="width: <%= prct %>%" role="progressbar" aria-valuenow="<%= family.owned %>" aria-valuemin="0" aria-valuemax="<%= family.total %>"></div>
              </div>
            </div>
          <% } %>
        <% } %>
      </div>
     <% } else { %>
       <em>This member hasn't collected any birds yet.</em>
     <% } %>
    </div>
  </div>

 <% if (member.birdyBuddy) { %>
  <script type="text/javascript">
    var message = $('.birdybuddy .message');

    $('#feedBug').on('click', function (event) {
      var button = this;

      if (!button.style.opacity) {
        button.style.opacity = .5;

        $.ajax({
          cache: false,
          url: `/api/nomnomnom`,
          method: 'POST',
          dataType: "json",
          contentType: 'application/json',
          data: JSON.stringify({
            birdypet: "<%= member.birdyBuddy.id %>"
          }),
          success: function(data) {
            if (!data.error) {
              message.text(data.response).show();
              button.childNodes[1].innerText = data.bugs;

              if (data.bugs == 0) {
                button.disabled = true;
              }
              else {
                button.style.opacity = null;
              }
            }
            else {
              button.innerText = data.error;
              button.disabled = true;
            }
          }
        });
      }
    });
  </script>
 <% } %>

<%- include('../footer.ejs') %>
